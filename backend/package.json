{
  "name": "whacked-backend",
  "private": true,
  "version": "1.0.0",
  "description": "Express API for Whacked (Render / local dev)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.3",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.22.1"
  }
}
